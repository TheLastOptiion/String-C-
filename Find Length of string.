int string_length(char *str){
              char str[]= "Hello to LastOPtion Repositories";
              int len=0;
              for(int i=0;str[i]!='\0';i++)
                  len++;
              return len;       
}

int main(){
   char X[]= "Hello to LastOPtion Repositories";                   
   cout<<solve(X);
}

** here X will automaticlly add '\0' at the last of string. 
